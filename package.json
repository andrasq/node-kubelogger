{
  "name": "kubelogger",
  "version": "0.9.2",
  "description": "fast easy k8s logger",
  "main": "index.js",
  "license": "Apache-2.0",
  "readme": "see README.md in the repo",

  "keywords": [ "k8s", "kubernetes", "kube", "logger", "stdout", "log", "quick" ],
  "author": "andrasq",
  "repository": { "type": "git", "url": "git@github.com:andrasq/node-kubelogger" },

  "dependencies": {
    "qlogger": "1.6.0"
  },

  "scripts":{
    "test": "qnit test.js",
    "coverage": "nyc -r text -r lcov npm test",
    "clean": "rm -rf .nyc_output/ coverage/ npm-debug.log"
  }
}
